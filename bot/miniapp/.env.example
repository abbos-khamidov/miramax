# Leave unset to use relative /api requests (proxied to the backend by Vite in dev,
# or by the reverse proxy in production). Only set this if the API is on a different origin.
# VITE_API_BASE_URL=https://api.example.com
